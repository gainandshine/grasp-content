name: Build Search Index

on:
  push:
    branches: [ main ]
    paths:
      - 'content/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-search:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Update Available Languages
        run: node scripts/update-available-languages.js

      - name: Generate Search Indexes
        run: node scripts/build-search.js

      - name: Commit Search Indexes and Content Metadata
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add search/*.json content/**/topic.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: auto-generate search indexes & update available languages [skip ci]"
            git pull --rebase origin main
            git push
          fi
